numpy
PyAudioWPatch   # PyAudio fork with WASAPI loopback; stock PortAudio cannot tap a playback device
pillow          # the 16px icon in the title bar
pynput          # the macros tab: global key/mouse hooks, and sending keystrokes back out
